{
  "db_instance_identifier": "payments-prod",
  "observations": [
    "One unindexed report query dominated DB load and CPU during the incident.",
    "The query plan performed repeated sequential scans over the orders table."
  ],
  "top_sql": [
    {
      "sql": "SELECT customer_id, sum(total_amount) FROM orders WHERE created_at >= now() - interval '90 days' GROUP BY customer_id ORDER BY sum(total_amount) DESC",
      "db_load": 93.5,
      "wait_event": "CPU"
    }
  ],
  "wait_events": [
    {
      "name": "CPU",
      "db_load": 88.2
    }
  ]
}
