{
  "db_instance_identifier": "payments-prod",
  "observations": [
    "Top SQL during the incident was bulk INSERT traffic from an audit table backfill.",
    "Wait events shifted toward IO:DataFileWrite as writes stalled on a full volume."
  ],
  "top_sql": [
    {
      "sql": "INSERT INTO audit_events_archive SELECT * FROM audit_events WHERE created_at >= now() - interval '14 days'",
      "db_load": 42.9,
      "wait_event": "IO:DataFileWrite"
    }
  ],
  "wait_events": [
    {
      "name": "IO:DataFileWrite",
      "db_load": 31.4
    }
  ]
}
